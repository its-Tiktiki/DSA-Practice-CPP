#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;

    // int a[10];
    // a[11] = 10; // its an undefined behaviour

    int a[n];
    for(int i = 0; i < n; ++i){
        cin >> a[i];
    }

    return 0;
}